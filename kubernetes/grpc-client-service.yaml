apiVersion: v1
kind: Service
metadata:
  name: grpc-client-service
  namespace: project
spec:
  selector:
    app: grpc-client
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081